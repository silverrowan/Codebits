---
tags:
  - Ruby
---
- **print :** 'prints' the indicated data to the screen. *Returns `nil`*
- **puts :**  'prints' the indicated data to the screen, and adds a new line (\n) at the end *Returns `nil`*
- p : 
- putc : 'prints' the *first character of* the indicated data to the screen... except when I test it I get a return: `d=>"data to display"` and when stored in a variable, stores the entire statement