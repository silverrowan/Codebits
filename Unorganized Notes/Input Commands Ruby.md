---
tags:
  - Ruby
---
- **gets :** when used, program waits for user input and after they press 'enter' program will continue its execution. *Returns ` {user input}\n`*
    - you can use .chomp to remove trailing `\n (& other separators)` 
    - or use it as a separator...